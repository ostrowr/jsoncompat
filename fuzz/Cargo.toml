[package]
name = "json_schema_fuzz"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8.5"
serde_json = "1.0.135"
# Depend on the backcompat crate for the SchemaNode definition and
# minimal validator that we reuse.
# The fuzz crate only depends on the core Draft‑2020 schema implementation for
# the `SchemaNode` AST.
json_schema_draft2020 = { path = "../schema" }

[dev-dependencies]
url = "2.5.4"
